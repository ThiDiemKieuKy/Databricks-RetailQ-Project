resources:
  dashboards:
    retail_sales_analysis_dashboard:
      display_name: "Retail Sales Performance Dashboard"
      file_path: ../src/dashboards/retail_sales_analysis_dashboard.lvdash.json
      # Optional: specify warehouse ID if required by your workspace configuration
      warehouse_id: ${var.warehouse_id}